{
  "name": "bookshelf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal catalog for books, movies, and TV — Editorial Dusk & Dawn",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "android:assets": "vite build && rm -rf android-app/app/src/main/assets/* && cp -r dist/* android-app/app/src/main/assets/"
  },
  "dependencies": {
    "dexie": "^4.0.8"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
